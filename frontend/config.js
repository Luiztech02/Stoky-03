/**
 * STOCKSYS — config.js
 * Aponta o frontend para a API. Troque aqui se o backend estiver
 * rodando em outro host/porta (ex: em produção).
 */
window.STOCKSYS_API_BASE = window.STOCKSYS_API_BASE || 'http://localhost:8000';
